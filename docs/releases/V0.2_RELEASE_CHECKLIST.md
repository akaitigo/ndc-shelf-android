# v0.2 データ所有権リリースチェックリスト

## 判定

**状態: BLOCKED（実機証跡待ち）**

自動検証はPull Requestごとに実行する。release candidateは正規の更新経路を試せる`versionCode = 4`、`versionName = "0.2.0"`とする。以下の手動ゲートを全て証跡付きで完了するまで、tag作成と配布を行わない。データ欠落、破損、説明との不一致が1件でもあればreleaseを停止してIssue化する。

## 対象

- 更新元: commit `d852975025bf5d224e29f5b9fc475cf8c0bff957`（公開時のv0.1.2、`versionCode = 3`）
- 更新先: v0.2 release candidate commit（`versionCode = 4`、検証時にSHAを記録）
- DB: v1から現行版までの全exported schema
- fixture: `fixtures/v0.2/anonymous-library-v1.json`
- 禁止データ: 実在する個人蔵書、個人名、自宅の置き場所、private URI

## 自動ゲート

- [x] `verifyBackupPolicy`: OS backup／端末間転送ruleをfail closedで検証する。
- [x] `verifyV02ReleaseConfiguration`: install可能なv0.2識別子（versionCode 4／versionName 0.2.0）を検証する。
- [x] `verifyLicenseReport`: release依存とライセンス本文を検証する。
- [x] `testDebugUnitTest`: import/export、backup/restore、Room migration、Repository、通信境界を検証する。
- [x] `lintDebug`: Android lint errorがない。
- [x] `assembleDebug`: install可能なdebug APKを生成する。
- [x] `git diff --exit-code -- app/schemas`: Room schema fixtureの未commit差分がない。
- [x] 匿名fixtureがJSON import、validation、JSON／CSV再export、CSV再importを通過する。

実行コマンド:

```bash
JAVA_HOME="$(mise where java@temurin-17.0.20+8)" \
  ./gradlew verifyV02ReleaseConfiguration verifyBackupPolicy verifyLicenseReport \
    testDebugUnitTest lintDebug assembleDebug
git diff --exit-code -- app/schemas
```

## 実機ゲート

検証端末、Android版、DocumentProvider名／版、release candidate SHA、実施者、日時を[証跡](#証跡)へ記録する。スクリーンショットにfixture以外のデータを含めない。

### v0.1.2からの更新

baseline APKのビルド・更新インストール・アプリdata保持・更新後の起動は、
`android.yml`の`update-install`ジョブがAPI 29エミュレーターで毎回検証する
（`.github/scripts/verify-update-install.sh`）。Room v1のDBが現行schemaで
破壊されないことは`LegacyDatabaseUpgradeInstrumentationTest`がAPI 26／29／35で
検証する。実機ゲートでは、これらが担保できない**実UI操作と実端末**だけを確認する。

- [x] baseline APK生成・`adb install -r`・アプリdata保持・更新後起動（CI: update-install）
- [x] v0.1.2で作成したDBが現行版で全項目を保持して開ける（CI: androidTest、API 26／29／35）
- [ ] 実機のv0.1.2 APKでUIから匿名の3冊（nullable項目、読書状態、置き場所を含む）を登録し、更新後に3冊の全項目と件数が画面上で保持される。
- [ ] 更新後に追加・編集・削除・取り消しを行い、再起動後も整合する。

### Storage Access Framework往復

最低2種類（例: Files by Googleの端末内storage、Google Driveまたは別のDocumentProvider）で行う。

- [ ] 匿名fixtureをJSON importし、追加2件・警告0件をpreviewして確定できる。
- [ ] JSON export→アプリdata初期化→JSON importで全16項目と件数が一致する。
- [ ] CSV export→別名保存→CSV importでUnicode、カンマ、改行、nullable項目が一致する。
- [ ] CSVを表計算アプリで開き、`=匿名サンプル図書A`がformulaとして実行されない。
- [ ] 保存・選択cancel、読取専用URI、provider切断で既存本棚が変化せず回復可能なerrorになる。

### 完全backup・restore

- [ ] 完全backup→蔵書変更→restoreで内部IDを含む元状態へ戻る。
- [ ] checksum改変、切断・書込失敗、容量不足で現在DBが変化しない。
- [ ] 形式版1のbackupを形式版2へ移行してrestoreできる。
- [ ] 新しい未知DB版をdata変更前に拒否する。
- [ ] restore直前の自動backup名を確認し、最大3世代に制限される。

### オフライン・説明照合

- [ ] 機内モードで既存本棚の表示、検索、編集、JSON／CSV exportが動作する。
- [ ] 未登録ISBNの取得失敗がofflineに分類され、再試行後に回復する。
- [ ] cache済み表紙は表示でき、未cache表紙の失敗で本棚操作が妨げられない。
- [ ] `PRIVACY.md`、アプリ内「情報」、`STORE_LISTING.md`、backup rule、実際の通信先が一致する。
- [ ] 実機の権限一覧が`CAMERA`と`INTERNET`の説明範囲を超えない。

## 最終release操作

手動ゲート完了後に別PRで次を実行する。

1. `versionCode = 4`、`versionName = "0.2.0"`と全実機証跡を再確認する。4が既に配布済みなら未使用のより大きい値へ上げ、更新試験を再実施する。
2. `CHANGELOG.md`の`Unreleased`を日付付き`0.2.0`へ確定する。
3. release buildと署名、commit SHA、artifact SHA-256を記録する。
4. tag `v0.1.2`が公開baseline commitを指すことを確認し、tag `v0.2.0`をrelease commitへ付ける。
5. 段階配布し、crash、ANR、data loss報告を監視する。保護設定や必須checkを回避しない。

## 証跡

| 項目 | 値 |
| --- | --- |
| Release candidate SHA | 未実施 |
| v0.1.2 APK SHA-256 | 未実施 |
| v0.2 APK/AAB SHA-256 | 未実施 |
| 端末 / Android | 未実施 |
| DocumentProvider 1 / version | 未実施 |
| DocumentProvider 2 / version | 未実施 |
| 実施者 / 日時 | 未実施 |
| 証跡保存先 | GitHub PR（fixture以外の個人データを含めない） |

## 受け入れ条件

- 自動・実機ゲートが全て成功し、証跡欄が埋まっている。
- data loss、部分import、既存DB破壊、privacy説明との不一致がない。
- 既知制約とrollback手順がrelease notesから参照できる。
- 最終version、tag、artifact SHA-256が同じrelease commitへ対応する。
