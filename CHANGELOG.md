# Changelog

このプロジェクトの利用者向け変更を記録する。形式は[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)を参考にし、リリース前の変更は`Unreleased`へ追加する。

## [Unreleased]

### Added

- 端末内で完結する言語モデルによるAI司書。推論は端末内だけで行い、蔵書・検索文・生成結果を外部へ送信しない。
- 配布物を通常版とAI版の2種類に分けた。通常版のダウンロードサイズと対応端末はこれまでどおり。AI版はAndroid 7.0以上の64bit Arm端末が対象。
- 「端末内モデルの管理」画面。モデルの取得、進捗表示、中止、SHA-256による整合性確認、削除、非対応端末での理由表示。
- 同意目的「AIモデルの取得」。モデル配布元への通信だけを対象とし、端末内推論を行うAI司書の同意とは分けている。送信先・送信項目・保存期間・第三者提供を同意画面で確認できる。

### Changed

- AI司書の回答は、モデル未導入時や非対応端末では従来の端末内規則ベースの提案へ切り替わる。

## [0.5.0] - 2026-08-01

### Added

- JSON／CSVによる蔵書のexport、検証付きimport、競合preview。
- Room全テーブルのchecksum付き完全backupと、事前検証・自動退避を伴う原子的restore。
- 書誌情報、NDC分類、読書状態、置き場所の編集と、所有copy単位の削除・取り消し。
- データ管理画面、アプリ内プライバシー説明、OSSライセンス一覧、データ出典。
- Room migration／Repository統合テストと匿名リリースfixture。
- 部屋・本棚・段の追加、改名、並べ替え、削除と、所有copyの階層的な置き場所割り当て。
- 段内の本を局所更新で並べ替え、詳細画面で左右の本と戻す位置を確認する機能。
- 同じ版を複数冊登録し、表示名・置き場所・読書状態・取得日時をコピー単位で管理する機能。
- 書店モードで所有冊数、欲しい、予約済みを端末内保存し、購入済みへの変更時に本棚へ追加する機能。
- 連続スキャンの試行履歴、再起動後のセッション回復、編集済みコピーを保護する個別・一括取り消し。
- 暗所・小型バーコード向けのトーチ、ピンチズーム、タップフォーカス、成功表示と権限案内。
- 複数シリーズ所属、巻ラベル、外伝・合本、安定表示順を保持するRoom v8基盤。
- シリーズごとの所有巻・既知巻・読了巻・最新所有巻と、確認済み本編だけを対象にした欠巻候補の一覧・詳細表示。
- 日本語巻表記・数字・ローマ数字・上下巻・外伝のシリーズ候補を、確認後に新規または既存シリーズへ一括登録する画面。
- 文庫・新装版・電子版を自動マージせず、書誌差分を確認して可逆に関連付ける作品グループとシリーズ代替設定。
- シリーズ名の送信を明示許可した場合だけ、NDL Searchで週1回まで新刊書誌候補を確認する機能。
- Room v12のlocal-first同期journal、削除tombstone、因果cursor、競合証跡、読み取り専用の同期状態表示。
- 「プライバシーと同意」画面（目的・送信先・送信項目・保存期間・第三者提供の表示、送信ペイロードの事前確認、目的別の同意・撤回）。シリーズ新刊の自動確認は同意なし・撤回後に外部通信を行わない。
- スキップ可能な初回オンボーディング（カメラ任意・NDL送信範囲・バックアップ方針の説明と、スキャン・手入力・インポートの3導線）。情報画面から再表示でき、本棚画面にNDC・置き場所・読書状態の文脈ヘルプを追加。
- 読書開始・中断・再開・読了・再読を読書セッション（Room v14）として記録し、部分日付・任意の評価・メモを端末内保存。詳細画面から追加・編集・削除・取り消しでき、完全backup形式v13へ含める。
- 「診断とサポート情報」画面（アプリ/OS/DB版・件数・同期状態の表示、allowlist方式の事象コード記録14日/200件、生成前に内容を選択できる診断ファイル）。自動送信・分析SDK・追跡IDなし、個人データは構造上記録不可。
- タグ（Room v15、作品単位・固定パレット色・統合・上限100件）と保存済み検索による蔵書の横断整理。本棚のタグAND絞り込み・長押し一括付与/解除・タグ管理画面を追加し、JSONエクスポートv4と完全backup形式v14で往復保持。
- 「未読の自然科学」「書斎にある」「去年買った」「#研究用」のような日本語での蔵書検索。解釈は端末内のルール変換だけで行い（外部送信・新規依存なし）、結果を検索欄下のチップで提示して個別に×で解除できる。解釈できない入力は従来どおりの部分一致検索。クラウド解釈は未提供で、同意画面には「提供時は検索文のみ送信し蔵書は送らない」旨を明示。詳細はdocs/NL_SEARCH.md。
- 分析タブに端末内集計だけの読書傾向（積読期間・NDC類・月別読了推移）と理由つきランダム再発見を追加。履歴不足時は必要データを説明し、候補の「対象外にする」と「分析リセット」、外部送信しない旨の明示、TalkBack向けのテキスト併記に対応。
- 任意のE2EE同期（既定OFF）。アカウント不要で、利用者が選んだフォルダへ端末鍵で暗号化した同期データだけを保存します。同意と保存先選択を経て有効化し、招待コードと確認コードの照合による端末追加、接続端末の一覧・最終同期表示・個別失効、保存先の全削除と完了確認、同期停止をデータ管理画面から操作できます。定期同期は同意時だけ登録され、同意なし・撤回後は通信・ファイルアクセス・鍵生成を行いません（Room v16）。
- 既定OFFのAI司書（本棚→「AI司書に相談」）。質問ごとに対象範囲（検索結果・選んだ本・タグ）と送信項目を選び、対象の冊名一覧・送信先・送信項目・除外項目を確認してから実行する。置き場所・読書状態・読書メモ・タグ名は既定で除外。回答は参照した本と「推測であり蔵書データは変更されない」旨を必ず併記し、蔵書を自動変更しない。1回30冊・1日20回・timeout・キャンセル・プロバイダ停止の分類に対応し、質問履歴は端末内20件で全件削除可。クラウドのAIプロバイダは未接続で外部送信は発生しない（新規依存なし）。詳細はdocs/adr/0007-optin-ai-librarian.md。

- 端末内LLM AI司書。**配布物を2種類に分けました。** 通常版（`ndc-shelf-vX.Y.Z.apk`、約18 MB）はこれまでどおりで端末内LLMを含みません。AI版（`ndc-shelf-vX.Y.Z-ai.apk`、約29 MB）は端末内LLM（LiteRT-LM、Apache-2.0）を含み、モデルを取得するとAI司書が自然文で提案します。**2つはアプリIDが異なる別アプリで、同時インストールできる代わりにデータを共有しません**（乗り換えはエクスポート→インポート）。署名鍵は同一です。モデル（Qwen3-0.6B、Apache-2.0、約475 MB）はAPKに含めず、目的・送信先・送信項目を確認して同意したうえで明示的に取得します。取得はHTTPS・許可host・許可ドメイン内1回だけのリダイレクト追従・サイズ上限・SHA-256照合を通り、検証に成功した場合だけ有効化します（失敗しても直前のモデルを保持）。推論は完全にオフラインで、質問文・蔵書・回答・推論時間は端末外へ出ません。Android 7.0未満・64bit Arm以外・メモリや空き容量が足りない端末では取得も起動もせず、規則ベースの提案に切り替わります。回答の日本語品質について配布元の公式な保証はなく、その旨を画面に明示します。詳細はdocs/adr/0009-on-device-llm-librarian.mdとdocs/LOCAL_LLM_MODELS.md。

- タブレット・横画面・分割画面・折りたたみ端末への適応レイアウト。ウィンドウ幅が600dp以上でナビゲーションを画面下部から左側のレールへ移し、840dp以上では本棚とシリーズを一覧＋詳細の2ペインで同時に表示します。大画面では本文の最大幅を制限して読みやすさを保ち、横画面・分割画面・折りたたみの姿勢変更でも検索文字列・選択中の本・スクロール位置を失いません。端末の種類ではなくウィンドウの大きさで切り替えるため、同じ端末でも分割画面の幅に応じて最適な表示になります。方針はdocs/ADAPTIVE_LAYOUT.md。

- 英語と日本語の2言語対応。全文言をリソース化し、既定（fallback）を英語（`values/`）、日本語を `values-ja/` に分離した。冊数・件数は `<plurals>` で英語の単複を正しく出し分け、日付・時刻は端末ロケールの書式で表示する。ISBNとNDCコードは言語非依存の識別子としてそのまま表示し、NDCの類名だけを訳す。インポート検証・バリデーション・通知・同期の受領メッセージまで端末の言語に追従する。方針と用語集はdocs/I18N.md。

### Changed

- 配布方針を無料OSSのGitHub Releases署名付きAPKへ変更（アプリストアへは公開しない）。リリースはpre-release→stableの2段階とし、APK・SHA256SUMS・署名証明書・SBOM・NOTICESを添付する。判断は[ADR 0008](docs/adr/0008-github-releases-apk-distribution.md)。
- リリースゲートの「旧版からの更新でデータが保持される」検証をCIへ自動化（v0.1.2 APKからの更新インストールとRoom v1 DBの引き継ぎをエミュレーターで毎回検証）。
- CIへ主要画面のライト・ダーク・大文字スクリーンショット回帰（Roborazzi golden比較）、API 26エミュレーターの互換性行列、手動登録→本棚→Activity再生成のE2Eテストを追加。実機検証との分離と証跡はdocs/DEVICE_TEST_MATRIX.mdへ定義。
- スクリーンショット回帰へロケール軸（英語・擬似ロケール `en-XA` / `ar-XB`）を追加し、翻訳キーの欠落・不要をCIで検出する `verify-translations.sh` をverifyジョブへ追加。
- ランチャーアイコンをadaptive icon（foreground/background/monochrome、安全領域準拠、Android 13テーマアイコン対応）へ移行。
- リリースビルドでR8によるコード縮小・リソース縮小を有効化し、署名鍵をGitHub release environment secretsから供給する再現可能な署名付きAAB生成（タグ整合検証・mapping/SBOM/checksums添付・承認ゲート付き）を自動化。

- 画面遷移をNavigation Composeの型安全routeへ移行し、版違い編集の対象を回転・プロセス再生成後も復元。無効・削除済みIDへのディープリンクは選択を残さず安全に処理。
- Androidクラウドbackupから全アプリデータを除外し、Android 9以降の端末間転送だけRoom DBを対象化。
- NDL Search通信の失敗分類、上限付きretry、明示的な再試行UIを追加。
- 表紙取得をNDLのHTTPS thumbnailへ限定し、最大50MiBの端末cacheを設定。
- v0.3.0 release candidateとしてversionCodeを5へ更新し、匿名E2E release gateを追加。

### Fixed

- 階層化した棚へ配置済みのcopyで、編集直後の取り消しが構造化path差分により失敗する問題を修正。
- CSV再import時に、数式記号の直前にある元データのアポストロフィを欠落させず復元。
- 完全restore失敗時も検証済み自動退避backupを最大3世代に制限。
- 本編集シートの文字入力中に全蔵書の段内filter・sortを繰り返さないよう最適化。
- CSV importは列数不一致でも表示上限100件に達した時点で解析を終了。
- 完全backupのmanifest形式版とpayload schema版が一致しないファイルを復元前に拒否。
- 完全backup形式v11とRoom v10で作品グループ・所属・シリーズ代替設定を退避・復元。
- 完全backup形式v12とRoom v11でシリーズ監視設定・書誌候補・通知状態を退避・復元。

### Security

- AI司書の送信ペイロードを構造化データとして組み立て、書誌文字列を指示文へ連結しない。固定のsystemInstruction以外を構築できないようにし、プロンプトインジェクション・除外項目の漏えい・確認なし送信の回帰テストを追加。
- importの入力上限、schema検証、CSV formula injection対策、URL allowlist、transaction rollbackを追加。
- backup復元でZIP構造、SHA-256、DB版、件数、参照整合性、空き容量を事前検証。
- 同期OFFを既定とし、完全backupから端末ID・counter・journal・鍵を除外して復元時に再登録を要求。

### Known limitations

- シリーズ候補とNDL新刊書誌は確定事実ではなく、ユーザー確認前にシリーズ所属へ保存しない。
- 欠巻は確認済みの明示的な本編だけを対象とし、未登録番号や外部の最新刊を推測しない。
- 新刊候補確認は明示的opt-inで、成功後は7日間隔を空ける。通信障害時は一時的に再試行し、発売日・網羅性・在庫を保証しない。
- シリーズ統合はv0.4の対象外。
- 同期の参照実装はフォルダ保存先だけで、OAuth等のホスト型backendは未提供。端末追加は招待コード入力方式（QRスキャンは未提供）。
- 全ての同期端末と手動バックアップを同時に失うと、保存先の暗号化データは復号できません（運営者による鍵復旧は提供しません）。端末失効は今後の同期を止めるもので、失効端末が既に取得したデータは遠隔削除できません。
- AI司書はクラウドのAIプロバイダへ接続しない。回答は端末内の規則ベースで、正確性を保証しない。実プロバイダ接続は費用負担者・送信先・保存期間を明示し、同意を取り直したうえで将来の版に追加する。

## [0.1.2] - 2026-07-25

### Added

- ISBN scan、NDL Search書誌取得、端末内Room本棚、検索、NDC分類表示の初回公開版。

[Unreleased]: https://github.com/akaitigo/ndc-shelf-android/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/akaitigo/ndc-shelf-android/compare/d852975025bf5d224e29f5b9fc475cf8c0bff957...v0.5.0
[0.1.2]: https://github.com/akaitigo/ndc-shelf-android/commit/d852975025bf5d224e29f5b9fc475cf8c0bff957
