# Security Policy

## Supported versions

公開リリース前は`main`ブランチのみを対象とします。公開後は最新の安定リリースをサポートします。

## Reporting a vulnerability

脆弱性やプライバシー上の問題は、公開Issueへ詳細を書き込まないでください。GitHub Security Advisoriesの「Report a vulnerability」から非公開で報告してください。

報告には、影響範囲、再現手順、確認したバージョン、可能なら緩和策を含めてください。受領後7日以内の初回応答を目標とします。

APIキー、署名鍵、実ユーザーの蔵書データは、Issue、ログ、スクリーンショット、Pull Requestへ含めないでください。

## 診断ファイルの安全な共有

障害報告に診断情報が必要な場合は、アプリ内「データ」→「診断とサポート情報」→「診断ファイルを作成」を使用してください。

- 診断ファイルは端末内でだけ生成され、自動送信されません。含める内容（バージョン・件数・同期状態・同意目的・事象コード）は生成前に選択できます。
- ISBN・タイトル・著者・置き場所・検索語・URI・tokenは仕組み上記録されません。共有前にファイルをテキストエディタで開き、内容を確認してください。
- 共有はGitHub Security Advisories（非公開報告）またはIssue添付で行えます。スクリーンショットには本棚の内容を含めないでください。
