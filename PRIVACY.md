# プライバシー

最終更新日: 2026-08-01

NDC Shelfは、アカウント、広告、開発者独自のアクセス解析・クラッシュ解析・同期サーバーを持たない、端末内中心の蔵書管理アプリです。外部SDKとサービスによる処理は以下に開示します。

## 端末内で処理・保存するデータ

- カメラ画像とバーコード認識は端末内で処理し、画像を保存または送信しません。
- ISBN、書誌情報、NDC、読書状態、置き場所、購入予定（ほしい・予約済み）は端末内のRoomデータベースへ保存します。連続スキャンを開始した場合は、直近20セッションを上限にセッション時刻、試行ISBN、結果、追加したコピーとの関連を保存します。書誌情報は履歴へ複製しません。
- 最後に使用した蔵書の検索語、読書状態フィルター、並び順は、次回起動時に復元するためアプリ専用領域へ保存します。外部送信やエクスポートはせず、Androidの設定でアプリデータを消去するかアンインストールすると削除できます。
- 開発者はこれらの蔵書データや利用状況を収集しません。
- アプリをアンインストールするか、Androidの設定からアプリデータを消去すると、端末内データを削除できます。事前に作成したファイルやOS管理のバックアップは、それぞれの保存先で削除してください。

## 外部への通信

ISBNを登録または書店モードで検索したとき、端末内に書誌情報がなければ、入力または読み取ったISBNだけをHTTPSで[国立国会図書館サーチ](https://ndlsearch.ndl.go.jp/help/api)へ送信します。シリーズの定期確認を明示的に有効にした場合だけ、対象シリーズ名と検索開始年をシリーズごとに週1回送信します。一時的な通信障害では1時間からの指数バックオフで失敗した確認を再試行しますが、同じ7日間に成功済みのシリーズは再送信しません。初回結果は通知せず、以後の初見書誌だけを候補として通知します。発売予定の完全性や正確性は保証しません。保存済み候補はオフラインで参照できます。表紙キャッシュは最大50MiBで、backupやexportに含めません。

書誌・表紙通信はNDL SearchのHTTPS hostだけを許可し、別hostへのredirectを追従しません。定期確認では明示したシリーズ名以外のタイトル、著者、NDC、読書状態、置き場所、購入予定、他の蔵書を送りません。通信先にはIPアドレス、時刻、User-Agentなど通常のHTTPS通信情報が伝わる場合があります。各通信先での取扱いは提供元の規約に従います。

## 任意の同期（既定オフ）

複数端末の同期は既定でオフです。有効にした場合だけ、利用者がAndroidのフォルダ選択画面で指定した保存先へ、端末内で生成した鍵で暗号化した同期データを書き込みます。開発者のサーバーへは送信せず、アカウント登録も行いません。

送信するのは暗号化済みのオブジェクトだけです。タイトル、ISBN、著者、NDC、読書状態、置き場所、購入予定、シリーズ名、変更内容は全て暗号文の内部にあり、保存先の提供者を含む第三者は復号できません。保存先から見えるのは、ファイル数・サイズ・更新時刻・端末公開鍵・署名・同期プロトコル識別子などのメタデータだけです。スキャン履歴、シリーズ監視設定、通知状態、認証情報、鍵素材は同期しません。

鍵はAndroid Keystoreの端末専用鍵（署名鍵と、他の鍵を暗号化するための鍵）で保護し、平文の鍵をファイルやバックアップ、ログへ書き出しません。新しい端末を追加するときは、既存端末が表示する一回限りの招待コードと、両方の端末に表示される確認コードの照合、既存端末での明示的な承認を必要とします。端末を失効させると以後の同期データは復号できなくなりますが、その端末が既に取得したデータを遠隔削除することはできません。全ての同期端末と手動バックアップを同時に失った場合、暗号化データを復元する手段はありません（パスワード再設定による復旧は提供しません）。

同期の停止（サインアウト）は端末の同期鍵を破棄して定期同期を止めます。端末内の蔵書データと保存先のデータはそのまま残ります。「保存先の同期データを全削除」を実行すると、選択フォルダ内の暗号化データ・鍵配布・端末一覧を削除し、完了結果を画面に表示します。保存先のごみ箱やクラウド側の複製は各保存先の規約に従います。各端末内のデータ削除は端末ごとの操作です。

シリーズ定期確認や同期などの任意外部通信は、「データ」画面の「プライバシーと同意」から目的別に同意状態を確認・撤回できます。初回有効化の前に、目的・送信先・送信項目・保存期間・第三者提供の有無と、実際に送信される項目を提示します。同意は目的ごとに独立しており、包括同意はありません。撤回すると該当機能の外部通信は直ちに停止し、ローカル機能は制限されません。送信内容の説明を変更した場合は、再同意されるまで外部通信を行いません。同意した版と日時は端末内にだけ記録され、完全バックアップには含まれません。

バーコード認識に使用するML Kitは、カメラ画像、バーコード値、認識結果をGoogleへ送信しません。一方、Googleの説明によると、ML Kit Android SDKは端末・アプリ情報、インストール単位の識別子、性能、API設定、入出力サイズ、機能バージョン、利用イベント、エラーコード等を診断と利用分析のため収集します。これらはHTTPSで暗号化され、Googleから第三者へ共有されないと説明されています。詳細は[ML Kitのデータ開示](https://developers.google.com/ml-kit/android-data-disclosure)と[規約・プライバシー](https://developers.google.com/ml-kit/terms)を確認してください。本アプリは自動ズームを有効にしていません。

これら以外に、蔵書や利用状況を開発者のサーバーへ送る機能はありません。外部リンクを選んだ場合はブラウザーが開き、リンク先のプライバシー条件が適用されます。

## AI司書

本棚の「AI司書に相談」は初期状態で無効です。有効にするには「プライバシーと同意」または相談画面で、目的・送信先・送信項目を確認したうえで個別に同意する必要があります。

現時点のAI司書は端末内だけで動作し、外部への送信は一切発生しません。クラウドのAIプロバイダは接続しておらず、接続機能は将来の版で、費用の負担者・送信先・保存期間・第三者提供の有無を明示し、同意を取り直したうえでのみ追加します。同意していない状態、または撤回後は、AI司書の処理そのものを実行しません。

送信対象は質問ごとに選びます。質問の直前に、対象の本の冊数と冊名一覧・送信先・送信する項目・送信しない項目を提示し、明示的に確認した場合だけ実行します。既定で送信するのは書名・著者・出版社・出版年・NDC分類だけで、置き場所、読書状態、読書メモ、タグ名は既定で除外します。これらを含めたい場合は質問ごとにチェックが必要です。ISBN、端末内の識別子（作品ID・コピーID）は送信対象に含めません。書誌情報は指示文とは別の構造化データとして扱い、書名や著者名に含まれる命令文を指示として解釈しません。1回の質問は最大30冊、1日あたり最大20回までに制限しています。

AI司書は提案を表示するだけで、蔵書データを自動的に変更しません。回答には必ず、参照した本の一覧と「この提案は推測です。蔵書データは変更されません。」を併記します。質問履歴は端末内のアプリ専用領域に最大20件だけ保存し、エクスポート、完全バックアップ、同期、Androidのクラウドバックアップには含めません。相談画面の「質問履歴をすべて削除」でいつでも全件削除できます。AI司書を無効にしても、本棚、検索、スキャン、分析などの機能は制限されません。

### 端末内LLM（AI版アプリのみ）

NDC Shelfは2種類のアプリを配布しています。

- **通常版（`ndc-shelf-vX.Y.Z.apk`）**: 端末内LLMを含みません。AI司書は規則ベースの提案だけを返します。
- **AI版（`ndc-shelf-vX.Y.Z-ai.apk`）**: 端末内LLMを含み、モデルを取得すると自然文で提案します。

2つは別アプリとして扱われるため同時にインストールでき、**データは共有しません**。乗り換えるときは、元のアプリでエクスポートし、新しいアプリでインポートしてください。

AI版で端末内LLMを使う場合も、次の境界を守ります。

- **推論は完全にオフラインです。** 質問文、蔵書の項目、AIへの指示文、回答、推論時間などの計測値は、端末外へ一切送信しません。推論処理はネットワーク機能を使いません。
- **モデルの取得だけが外部通信です。** 「端末内モデルの管理」で目的・送信先・送信項目を確認して同意し、「モデルを取得」を実行したときだけ、モデルの配布元（`huggingface.co`）へモデルファイルを要求します。このとき送信するのは、アプリに埋め込まれた配布元URLとアプリ名・版を示すUser-Agentだけです。蔵書、質問文、回答、端末の識別子は送信しません。cookieや認証情報も付けません。この同意はAI司書への相談（端末内で完結し通信しません）とは別で、いつでも個別に撤回できます。
- **リダイレクトは配布元のドメイン内で1回だけ追います。** 配布元はファイルの実体を別のサーバー（CDN）へ置いているためです。それ以外への誘導は拒否します。
- **取得したファイルはサイズとSHA-256が登録値と一致した場合だけ取り込みます。** 一致しないファイルは破棄し、すでに端末内にあるモデルはそのまま残ります。
- **モデルは端末内のアプリ専用領域にだけ保存します。** Androidのクラウドバックアップ、端末間移行、エクスポート、同期の対象外です。「モデルとキャッシュを削除する」でいつでも全削除できます。
- **端末内の診断記録に残るのは、モデルID・モデル版・モデルのハッシュ値の先頭16桁・推論エンジン名と版・所要時間・文字数・失敗の分類だけです。** 質問文、書誌、回答、AIへの指示文は記録しません。
- **対応しない端末では機能そのものを提供しません。** Android 7.0（API 24）未満、64bit Arm以外のCPU、メモリや空き容量が足りない端末では、モデルの取得も推論の開始も行わず、規則ベースのAI司書に切り替わります。
- **回答の正確さは保証しません。** 採用しているモデルについて、配布元は日本語の品質に関する公式な説明をしていません。提案には常に参照した本の一覧と「この提案は推測です。蔵書データは変更されません。」を併記します。

## バックアップとエクスポート

JSON、CSV、完全バックアップは、利用者がAndroidのファイル選択画面で指定した保存先にだけ書き出します。アプリがこれらを独自にアップロードすることはありません。ファイルにはタイトル、ISBN、読書状態、置き場所などが含まれ、完全バックアップには購入予定、スキャン履歴、シリーズ監視設定、保存済み候補、通知状態も含まれます。JSONとCSVは所有する蔵書だけを対象とし、購入予定、スキャン履歴、シリーズ監視データは含みません。アプリ独自の暗号化は行わないため、端末や保存先の暗号化・アクセス制御で保護してください。

Androidのクラウドバックアップからは、蔵書を含む全アプリデータを除外しています。Android 9（API 28）以降の端末間転送ではRoomデータベースだけを移行対象にします。Android 8.1（API 27）以前はクラウドと端末間転送を安全に分けられないため、OS管理の移行も対象外です。端末間転送の実際の方式と保護はAndroidおよび端末メーカーの実装に従います。重要な移行では手動完全バックアップも作成してください。

過去のアプリ版でAndroidクラウドへ保存済みのバックアップは、この更新だけでは直ちに削除できない場合があります。保持期間と削除方法はAndroid、端末メーカー、利用中のバックアップサービスおよびアカウント設定に従います。

## 権限

- `CAMERA`: ISBNバーコードを端末内で読み取るために使用します。カメラがなくてもISBNを手入力できます。
- `INTERNET`: NDL Searchの書誌情報と表紙画像の取得、およびML Kitの上記通信に使用されます。
- `POST_NOTIFICATIONS`: 明示的に定期確認を有効にしたシリーズの新しい書誌候補を通知します。拒否した場合は定期確認を有効にしません。

## 問い合わせ

一般的な不具合や要望は[GitHub Issues](https://github.com/akaitigo/ndc-shelf-android/issues/new/choose)へ報告できます。秘密情報や実際の蔵書データを含めないでください。

脆弱性やプライバシー上の問題は公開Issueへ書かず、[GitHub Security Advisories](https://github.com/akaitigo/ndc-shelf-android/security/advisories/new)から非公開で報告してください。詳しくは[SECURITY.md](SECURITY.md)を参照してください。
